<!DOCTYPE html>

<html>
    <head>
        <title>Gestionnaire de contenu</title>
		<meta http-equiv="content-type" content="text/html; charset=utf-8" />
		<meta name="description" content="" />
		<meta name="keywords" content="" />
			<link rel="stylesheet" href="/css/skel.css" />
			<link rel="stylesheet" href="/css/style.css" />
			<link rel="stylesheet" href="/css/style-wide.css" />
	</head>

    <body>
    <div class="container contentManagement">
    <% 
    if(article != null && article != "") { 
    article = article[0];
    %>
    <p class="pageTitle">Vous éditez le contenu  : Référence : <%= article.reference %>  Titre : <%= article.title %></p>
    <% } else   { %>
    <p>Vous créer un nouveau contenu sous la référence: <%= reference %></p>
    <% } %>
		<form method="post" action="/contentManagment/update/">
			<div class="row half">
				<div class="6u">
					<label for="title">Titre</label>
	                <input type="text" name="title" id="title" value="<%= (article != null && article != '')?article.title:'' %>" autofocus />
				</div>
			</div>
			<div class="row half">
				<div class="6u">
					<label for="description">Description</label>
	         		<textarea rows="1" name="description" id="description" autofocus><%= (article != null && article != '')?article.description:''%> </textarea>
				</div>
			</div>
			<div class="row half">
				<div class="12u">
					<label for="content">Contenu</label>
					<textarea rows="2" name="content" id="content" autofocus /><%= (article != null && article != "")?article.content:"" %></textarea>
				</div>
			</div>
			<div class="row">
				<div class="12u">
					<input type="hidden" name="reference" value="<%=(article != null && article != "")?article.reference:reference %>" />
					<input type="submit" value='<%=(article != null && article != "")?"Modifier le contenu":"Creer l\'article"%>' />
				</div>
			</div>
		</form>
		</div>
    </body>
</html>